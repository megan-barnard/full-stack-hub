import styled from "styled-components";

const Notifications = () => {
  return (
    <Wrapper>
      Notifications
    
    </Wrapper>
  )
};

const Wrapper = styled.div`


`;

export default Notifications;