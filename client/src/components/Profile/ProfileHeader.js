import styled from "styled-components";

const ProfileHeader = () => {
  return (
    <Wrapper>
      ProfileHeader
    
    </Wrapper>
  )
};

const Wrapper = styled.div`


`;

export default ProfileHeader;