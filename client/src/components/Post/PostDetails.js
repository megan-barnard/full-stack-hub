import styled from "styled-components";

const PostDetails = () => {
  return (
    <Wrapper>
      PostDetails
    
    </Wrapper>
  )
};

const Wrapper = styled.div`


`;

export default PostDetails;