import styled from "styled-components";

const PostComment = () => {
  return (
    <Wrapper>
      PostComment
    
    </Wrapper>
  )
};

const Wrapper = styled.div`


`;

export default PostComment;